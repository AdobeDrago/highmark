/**
 * hero-minimal-light — purely visual block.
 * Light-blue tinted full-width band with a centered navy heading + intro
 * paragraph. No background image, no buttons. All styling handled in CSS;
 * no decoration needed.
 */
// eslint-disable-next-line no-unused-vars
export default function decorate(block) {}
